optimizer:
  _target_: torch.optim.AdamW
  weight_decay: 4.2e-06

scheduler:
  _target_: torch.optim.lr_scheduler.ReduceLROnPlateau
  mode: min
  factor: 0.5
  patience: 10

encoder_lr: 1e-5
head_lr: 2e-4

min_epochs: 1
max_epochs: 512
patience: 20