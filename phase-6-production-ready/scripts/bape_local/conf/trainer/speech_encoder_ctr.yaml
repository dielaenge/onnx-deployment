optimizer:
  _target_: torch.optim.AdamW
  lr: 0.0001
  weight_decay: 0.00005

scheduler:
  _target_: torch.optim.lr_scheduler.ReduceLROnPlateau
  mode: min
  factor: 0.5
  patience: 8
  threshold: 0
  cooldown: 3
  min_lr: 1.0e-6

temperature: 0.1

min_epochs: 1
max_epochs: 512
patience: 16